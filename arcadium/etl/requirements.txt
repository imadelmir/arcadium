# Driver PostgreSQL (API v3; incapsulato in src/db/connection.py).
# [binary] = wheel self-contained con libpq incluso, nessuna compilazione.
psycopg[binary]>=3.2

# Gestione delle variabili d'ambiente (file .env)
python-dotenv>=1.0.1

# Parsing in streaming del JSON: il dataset e' ~500MB, va letto a pezzi
# senza caricarlo tutto in memoria.
ijson>=3.3.0

# --- Test (M3-T7) ---
# Framework per i test automatici in tests/.
pytest>=8.0
