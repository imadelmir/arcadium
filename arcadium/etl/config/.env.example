# Template di configurazione della pipeline ETL.
# Copiare questo file in "config/.env" e inserire i valori reali.
# Il file .env (con le credenziali vere) NON va versionato.

# --- Connessione a PostgreSQL ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=arcadium
DB_USER=postgres
DB_PASSWORD=ACE_5

# --- Dataset di input (dentro etl/data/) ---
DATASET_FILE=steam_games.json
