# Copy this file to ".env.local" and fill in the values.
# Base URL of the Spring Boot backend (M4). The pages will read this
# when they start calling the API in later tasks.
NEXT_PUBLIC_API_URL=http://localhost:8080
