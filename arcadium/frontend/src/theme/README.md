# theme/

Design tokens for the whole app.

- `theme.css` - colours (violet palette), spacing, radius as **CSS variables**.
- `colors.js` - the same colours as a **JavaScript object** (for charts, etc.).

Import the CSS once in the root layout; import `colors` where you need them in JS.
