// Language settings for the whole app (M5 - T3).
// One source of truth: change a language here and it updates everywhere.

export const LANGUAGES = ["it", "en"];
export const DEFAULT_LANGUAGE = "it";

// Where we remember the user's choice between visits (browser only).
export const LANGUAGE_STORAGE_KEY = "arcadium-lang";

// Friendly names shown in the language switcher.
export const LANGUAGE_LABELS = {
  it: "Italiano",
  en: "English",
};
