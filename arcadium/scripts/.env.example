# scripts/.env.example
# Copia questo file in scripts/.env e metti una password vera.
# scripts/.env NON va committato: aggiungilo a .gitignore.
# Usata sia dal container db (per creare l'utente) sia da Flyway (per connettersi).
POSTGRES_PASSWORD=cambia_questa_password
